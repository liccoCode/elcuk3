{
    "id": "${fee.id}",
    "feeType": {
        "name": "${fee.feeType.name}",
        "nickName": "${fee.feeType.nickName}",
        "memo": "${fee.feeType.memo}"
    },
    "amount": "${fee.amount}",
    "currency": "${fee.currency}",
    "unitPrice": "${fee.unitPrice}",
    "unitQty": "${fee.unitQty}",
    "memo": "${fee.memo}",
    "averagePrice": "${fee.averagePrice()}",
    "shipItem": {
        "id": "${fee.shipItem?.id}",
        "weight": "${fee.shipItem?.weight?:0}"
    },
    "payee": {
        "username": "${fee.payee.username}"
    }
}